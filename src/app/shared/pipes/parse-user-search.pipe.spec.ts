import { ParseUserSearchPipe } from './parse-user-search.pipe';

describe('ParseUserSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseUserSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
