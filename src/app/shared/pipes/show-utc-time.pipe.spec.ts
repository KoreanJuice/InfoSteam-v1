import { ShowUTCTimePipe } from './show-utc-time.pipe';

describe('ShowUTCTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowUTCTimePipe();
    expect(pipe).toBeTruthy();
  });
});
