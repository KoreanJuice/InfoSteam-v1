import { ParseSearchPipe } from './parse-search.pipe';

describe('ParseSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
