import { CleanSearchPipe } from './clean-search.pipe';

describe('CleanSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
