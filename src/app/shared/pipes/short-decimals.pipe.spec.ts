import { ShortDecimalsPipe } from './short-decimals.pipe';

describe('ShortDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
